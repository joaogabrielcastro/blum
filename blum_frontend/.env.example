# URL base da API (obrigatório terminar em /api/v2). Defina antes de `npm run build`.
# Local:
REACT_APP_API_URL=http://localhost:3011/api/v2

# Produção (exemplo — ajuste ao seu domínio):
# REACT_APP_API_URL=https://api-blum.jwsoftware.com.br/api/v2
