# Database Configuration
DATABASE_URL=postgresql://user:password@host/database

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3001

# JWT Secret (CHANGE IN PRODUCTION!)
JWT_SECRET=blum-secret-key-change-in-production-use-long-random-string

# Gemini AI API Key (Optional - for PDF/CSV intelligent import)
GEMINI_API_KEY=your_gemini_api_key_here

# Security Notes:
# 1. NEVER commit the real .env file to Git
# 2. Change JWT_SECRET to a long random string in production
# 3. Rotate DATABASE_URL and GEMINI_API_KEY if they were exposed
# 4. Use environment variables in production (Heroku, Render, Vercel, etc.)
